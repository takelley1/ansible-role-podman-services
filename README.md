Role for all container services.

This role requires the netaddr Python package.
See: https://docs.ansible.com/ansible/latest/user_guide/playbooks_filters_ipaddr.html#ip-math
```bash
pip install -r requirements.txt
```
